# Kafka QA Realistic Testing

Проект для тестирования Kafka в реальных условиях. Содержит продюсер и консьюмер с обработкой ошибок, DLQ и event-driven архитектурой.

---
В проекте заложены изначально ошибки и дублирование сообщений. 
---
В проекте можно смотреть логи в консоли или Docker Desktop.
Так же можно самостоятельно отправить сообщения через Kafka UI.


## 🚀 Запуск
0. **Перейти в папку с проектом:**
   ▶️ `cd` и путь к вашему проекту

1. **Запустить Kafka и UI:**
   ▶️ `docker-compose up -d`
   > ⏳ Ожидание: 30–60 секунд. Проверьте в Docker Desktop — все сервисы должны быть в статусе *Running*.

2. **Открыть Kafka UI:**  
   🔗 [http://localhost:8080](http://localhost:8080)

3. **Создать топики вручную (3 партиции):**  
   - `orders.process`  
   - `payment.process`  
   - `orders.process.dlq`  
   > 💡 Как: в Kafka UI → *Topics* → *Create Topic* → укажите имя и `3` в поле *Partitions*.

4. **Запустить продюсера:**  
   ▶️ `docker-compose run --rm producer`  
   > ✅ Отправляет 7 тестовых сообщений (включая ошибки и дубли).

5. **Запустить консьюмер:**
   ▶️ `docker-compose run --rm consumer`  
   (Для остановки консьюмера: `Сtrl+C`)
   > ✅ Обрабатывает сообщения, отправляет в `payment.process`, логирует ошибки в DLQ.

---

## 🛠️ Как обновить проект после изменений кода

6. **Остановить контейнеры:**  
   ▶️ `docker-compose down`

 !!!После остановки Kafka все будет очищено - топики, сообщения !!!
---

7. **Пересобрать образ (важно!):**  
   ▶️ `docker-compose build`  
   > 💡 Это обновляет образ с вашими изменениями в `producer_consumer.py`, `test_producer.py` и др.  
   > ❌ Не нужно `--rmi all` или `--volumes` — это избыточно и опасно.

8. **Запустить с новым образом:**  
   ▶️ `docker-compose up -d`  
   > ✅ Теперь используется свежий код.

---

## 🧪 Дополнительные инструменты (по желанию)

9. **Тестовый продюсер (сценарии):**  
   ▶️ `docker-compose run --rm test-producer`  
   > Проверяет: дубли, партиции, битые JSON, отсутствующие поля.

10. **Анализ партиций:**  
    ▶️ `docker-compose run --rm partition-analyzer`  
    > Показывает: распределение по партициям, порядок offset’ов, ключи.

11. **Проверка DLQ:**  
    ▶️ `docker-compose run --rm dlq-checker`  
    > Автоматически выводит: сколько ошибок, какие причины, какие `order_id` попали в DLQ.

---

## 📋 Что нужно на ПК

- ✅ Docker Desktop (Windows 10+)
- ✅ PowerShell или CMD
- ✅ Свободные порты `9092` и `8080`
- ✅ Папка проекта с файлами:
