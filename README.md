# Kafka QA Realistic Testing

Проект для тестирования Kafka в реальных условиях. Содержит продюсер и консьюмер с обработкой ошибок, DLQ и event-driven архитектурой.

---

## 🚀 Запуск

1. **Запустить Kafka и UI:**  
   ▶️ `docker-compose up -d`

2. **Запустить продюсера и консьюмер:**  
   Открыть Kafka UI:  
   🔗 [http://localhost:8080](http://localhost:8080)

3. **Создать топики вручную (3 партиции):**  
   - `orders.process`  
   - `payment.process`  
   - `orders.process.dlq`  
   > 💡 Как создать: в Kafka UI → *Topics* → *Create Topic* → укажите имя и `3` партиции.

4. **Запустить продюсера:**  
   ▶️ `docker-compose run --rm producer`

5. **Запустить консьюмер:**  
   ▶️ `docker-compose run --rm consumer`

---

## 🧪 Дополнительные инструменты (по желанию)

6. **Запустить тестовый продюсер (сценарии):**  
   ▶️ `docker-compose run --rm test-producer`  
   > Проверяет: дубли, партиции, ошибки валидации.

7. **Анализ партиций:**  
   ▶️ `docker-compose run --rm partition-analyzer`  
   > Показывает распределение сообщений по партициям и порядок offset’ов.

8. **Проверка DLQ:**  
   ▶️ `docker-compose run --rm dlq-checker`  
   > Автоматически проверяет, все ли ошибочные сообщения попали в `orders.process.dlq`.

---

## 📋 Что нужно на ПК

- ✅ Docker Desktop (Windows 10+)
- ✅ PowerShell или CMD
- ✅ Свободные порты `9092` и `8080`
- ✅ Папка проекта с этими файлами: