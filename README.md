# Kafka QA Realistic Testing

Проект для тестирования Kafka в реальных условиях. Содержит продюсер и консьюмер с обработкой ошибок, DLQ и event-driven архитектурой.

## 🚀 Запуск

1. **Запустить Kafka и UI:**
   ```bash
   docker-compose up -d
   ```

2. **Запустить продюсера и консьюмер:** Открыть Kafka UI:
http://localhost:8080

3. **Создать топики вручную (3 партиции):**
orders.process
payment.process
orders.process.dlq
4. **Запустить продюсера:**
    ```bash
    docker-compose run --rm producer
    ```

5. **Запустить консьюмер:**
    ```bash
    docker-compose run --rm consumer
    ```
## 7. 🚀 `run_tests.sh` (опционально)

```bash
#!/bin/bash
echo "🚀 Запуск Kafka..."
docker-compose up -d

echo "⏳ Ожидание запуска Kafka (30 секунд)..."
sleep 30

echo "📤 Запуск продюсера..."
docker-compose run --rm producer

echo "👂 Запуск консьюмера..."
docker-compose run --rm consumer

echo "✅ Тестирование завершено"
echo "Откройте http://localhost:8080 для просмотра результатов"